export function getReactViteStackBlock(): string[] {
  return [
    "### React + Vite Rules",
    "",
    "Preserve provider composition in `src/main.tsx`.",
    "Route wiring belongs in routing modules, not scattered through components.",
  ];
}
