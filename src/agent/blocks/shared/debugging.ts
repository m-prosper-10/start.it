export function getDebuggingBlock(): string[] {
  return [
    "## Debugging",
    "",
    "Identify root cause before proposing fixes.",
    "Do not rewrite large sections of code blindly.",
    "Prefer minimal targeted fixes.",
  ];
}
